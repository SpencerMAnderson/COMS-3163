// create a function countTo (num) that will print all numbers up to the parameter.

"use strict";


function countTo(num) {
    for (let i = 0; i <= num; i++){
        console.log(i);
    }
  }
  countTo(42);